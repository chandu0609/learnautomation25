package com.crm.qa.util;

import java.io.IOException;
import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.events.WebDriverListener;
import org.testng.ITestListener;
import org.testng.ITestResult;

import com.crm.qa.base.TestBase;

public class WebEventListener extends TestBase implements WebDriverListener, ITestListener {

	/*
	 * @Override public void onTestFailure(ITestResult result) { try {
	 * TestUtil.takeScreenShot(); } catch (IOException e) { // TODO Auto-generated
	 * catch block e.printStackTrace(); } }
	 * 
	 * 
	 * @Override public void beforeClick(WebElement element) {
	 * System.out.println("Before clicking on: " + element); }
	 * 
	 * @Override public void afterClick(WebElement element) {
	 * System.out.println("After clicking on: " + element); }
	 * 
	 * @Override public void onError(Object target, Method method, Object[] args,
	 * InvocationTargetException e) {
	 * System.out.println("⚠️ Exception occurred in method: " + method.getName());
	 * System.out.println("Target object: " + target);
	 * System.out.println("With arguments: " + java.util.Arrays.toString(args));
	 * System.out.println("Cause: " + e.getCause()); // Get real exception try {
	 * TestUtil.takeScreenShot(); } catch (IOException e1) { // TODO Auto-generated
	 * catch block e1.printStackTrace(); } }
	 */
}
    
    
	
